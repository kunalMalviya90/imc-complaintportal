# MongoDB Atlas URI (Production)
# Add your database name before the ? symbol
MONGODB_URI=mongodb+srv://imcadmin:imcadmin@cluster0.ahbqtbh.mongodb.net/imc_complaints?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration
PORT=3000
NODE_ENV=production

# Session Secret (Change this to a random secure string in production)
SESSION_SECRET=imc-secret-2024-change-this-in-production

# Email Configuration (Optional - for OTP feature)
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password
